---
layout: layouts/base.njk
title: Home
---

<!-- Hero Banner -->
<section class="aa-hero">
  <div class="aa-hero__bg"></div>
  <div class="aa-hero__inner">

    <div class="aa-hero__badge">
      <img src="/images/logo-badge.svg" alt="Archie’s Adventures" width="64" height="64">
    </div>

    <h1 class="aa-hero__title">
      Welcome to <span class="aa-accent">Archie’s Adventures!</span> 🌍
    </h1>

    <p class="aa-hero__lead">
      Join me as I explore amazing places, discover cool things, and share incredible stories!  
      Every adventure is a chance to learn something new.  
      Ready to explore with me?<span class="aa-pop">Let’s go! 🚀</span>
    </p>

    <div class="aa-hero__actions">
      <a href="/adventures/" class="btn-2tone aa-btn-lg">
        <span class="aa-ico">+</span> Start New Adventure
      </a>
      <a href="/adventures/" class="aa-btn-outline aa-btn-lg">
        <span class="aa-ico">🔍</span> Explore Adventures
      </a>
    </div>

  </div>
</section>

<!-- Latest Adventures -->
<section class="container">
  <h1>All Adventures</h1>
  <div class="grid">
    {% for p in collections.posts | reverse %}
      <article class="card">
        <a href="{{ p.url }}">
          {% if p.data.cover %}<img src="{{ p.data.cover }}" alt="">{% endif %}
          <h3>{{ p.data.title }}</h3>
          {% if p.data.excerpt %}<p class="meta">{{ p.data.excerpt }}</p>{% endif %}
          <p class="meta">{{ p.date | date("MMM d, yyyy") }}</p>
        </a>
      </article>
    {% endfor %}
  </div>
</section>
