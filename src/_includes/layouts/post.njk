{% extends "layouts/base.njk" %}
{% block content %}
<article>
  <h1>{{ title }}</h1>
  {% if cover %}<img src="{{ cover }}" alt="" style="margin:.5rem 0 1rem">{% endif %}
  <div class="content">{{ content | safe }}</div>
  <hr class="dash">
  <p><strong>Want to come along on my next adventure?</strong> Pop your email in the box below and I’ll send an update when a new story goes live! 🌍</p>
</article>
{% endblock %}
